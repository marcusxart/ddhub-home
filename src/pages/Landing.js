import React from "react";
import Main from "../components/main/Main";

const Landing = ({ mobileSize }) => {
  return (
    <>
      <Main mobileSize={mobileSize} />
    </>
  );
};

export default Landing;
